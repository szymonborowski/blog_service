apiVersion: batch/v1
kind: Job
metadata:
  name: blog-migration
  namespace: portfolio
  labels:
    app.kubernetes.io/name: blog-migration
    app.kubernetes.io/part-of: portfolio
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blog-migration
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: ghcr.io/decybell/microservices-blog:v0.0.1
          command: ["php", "artisan", "migrate", "--force"]
          envFrom:
            - configMapRef:
                name: blog-config
            - secretRef:
                name: blog-secret
